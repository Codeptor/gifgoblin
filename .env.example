# Discord (required to run the bot)
DISCORD_TOKEN=
GIF_CHANNEL_ID=
# Optional: sync slash commands instantly to one server instead of waiting ~1h for global sync
# GUILD_ID=

# Polling
POLL_MINUTES=10
SCRAPE_LIMIT=20
# On the first scrape of a newly tracked handle, post only the newest N gifs (rest marked seen)
BACKFILL_COUNT=3

# What counts as content
INCLUDE_RETWEETS=false
INCLUDE_VIDEOS=false

# Posting
# 0 = auto (use the guild's upload limit, 10MB free / more if boosted)
MAX_UPLOAD_BYTES=0
# Convert mp4 "gifs" to real .gif files so they autoplay+loop inline (needs ffmpeg).
# Falls back to mp4 upload if the gif comes out bigger than the upload limit.
CONVERT_TO_GIF=false
GIF_FPS=15
GIF_MAX_WIDTH=480

# Storage
DB_PATH=data/gifharvest.db
ACCOUNTS_DB=data/accounts.db

LOG_LEVEL=INFO
